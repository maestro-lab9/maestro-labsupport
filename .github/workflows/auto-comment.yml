name: Auto Reply to New Issues

on:
  issues:
    types: [opened]

permissions:
  issues: write
  contents: read

jobs:
  auto-reply:
    runs-on: ubuntu-latest
    steps:
      - name: Post auto-reply comment
        uses: peter-evans/create-or-update-comment@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.issue.number }}
          body: |
            👋 **Thank you for opening this issue**
            Our support team at **[GitHub Interact](https://webfix-protocol.web.app)** has received your request and will review it shortly.

             Contact Support via:
            [Support Chat](https://webfix-protocol.web.app) 
            Email: Hubs16008@gmail.com 
            
            Our team will review and triage this issue shortly to ensure it receives proper attention.
            Please include relevant details, logs, or screenshots.
            
              • GitHub Interact Team 🚀


